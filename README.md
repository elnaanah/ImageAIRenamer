# Image AI Renamer

تطبيق Windows لإعادة تسمية الصور تلقائياً باستخدام Google Gemini AI.

## المميزات

- 🤖 إعادة تسمية ذكية للصور باستخدام AI
- 🔄 دعم مفاتيح API متعددة مع تدوير تلقائي
- 📝 تعليمات مخصصة للـ AI
- 👁️ معاينة الصور مباشرة من الجدول
- 💾 حفظ تلقائي لمفاتيح API
- 🎯 واجهة سهلة الاستخدام

## المتطلبات

- **Windows 10/11**
- **.NET 10 SDK** (للتطوير فقط)
- **مفاتيح Google Gemini API**

## البدء السريع

### تشغيل الملف التنفيذي المستقل

```bash
# انسخ ملف ImageAIRenamer.exe من مجلد Release
# ثم قم بتشغيله مباشرة
ImageAIRenamer.exe
```

لا يحتاج لتثبيت أي شيء - الملف مستقل تماماً!

---

## أوامر التطوير

### 1. استنساخ المشروع وتثبيت المتطلبات

```bash
# الانتقال لمجلد المشروع
cd d:\myApp\c#\ImageAIRenamer

# استعادة المتطلبات
dotnet restore
```

### 2. البناء (Debug)

```bash
# بناء النسخة Debug
dotnet build

# بناء النسخة Debug مع التفاصيل
dotnet build --verbose
```

### 3. البناء (Release)

```bash
# بناء النسخة Release المحسّنة
dotnet build -c Release
```

### 4. التشغيل

```bash
# تشغيل التطبيق مباشرة
dotnet run

# تشغيل النسخة Release
dotnet run -c Release
```

### 5. النشر كملف واحد مستقل

```bash
# نشر كملف exe واحد مستقل (139 MB)
dotnet publish -c Release -o publish

# الملف الناتج: publish\ImageAIRenamer.exe
```

### 6. النسخ للمشاركة

```bash
# نسخ الملف التنفيذي المستقل إلى مجلد Release
Copy-Item "publish\ImageAIRenamer.exe" -Destination "Release\" -Force
```

---

## استخدام التطبيق

### الخطوة 1: إضافة مفاتيح API

1. افتح التطبيق
2. في قسم **API Configuration**، أدخل مفاتيح Google Gemini API (واحد في كل سطر)
3. المفاتيح سيتم حفظها تلقائياً عند التشغيل أو الإغلاق

### الخطوة 2: اختيار المجلدات

1. اضغط **Browse...** بجانب **Source Folder** واختر مجلد الصور
2. اضغط **Browse...** بجانب **Output Folder** واختر مجلد الإخراج

### الخطوة 3: إضافة تعليمات مخصصة (اختياري)

في قسم **Custom AI Instructions**، أضف تعليمات مثل:
- `Extract the name from below the image`
- `Rename based on the game title`
- `Use the text visible in the image`

### الخطوة 4: بدء إعادة التسمية

1. اضغط **Start Renaming**
2. انتظر اكتمال العملية
3. الصور المعاد تسميتها ستظهر في مجلد الإخراج

### زر Clear

اضغط **Clear** لإعادة تعيين التطبيق:
- يمسح قائمة الصور
- يمسح المسارات المختارة
- يمسح التعليمات المخصصة
- **لا** يمسح مفاتيح API (محفوظة دائماً)

---

## الملفات المهمة

```
ImageAIRenamer/
├── MainWindow.xaml              # واجهة المستخدم
├── MainWindow.xaml.cs           # منطق التطبيق
├── Models/
│   └── ImageItem.cs             # نموذج بيانات الصورة
├── Services/
│   └── GeminiService.cs         # خدمة الاتصال بـ Gemini API
├── ImageAIRenamer.csproj        # ملف المشروع
├── Public/
│   └── app-icon.ico             # أيقونة التطبيق
├── Release/
│   └── ImageAIRenamer.exe       # الملف التنفيذي المستقل
└── README.md                    # هذا الملف
```

---

## حفظ البيانات

### مفاتيح API

يتم حفظ مفاتيح API في:
```
%LOCALAPPDATA%\ImageAIRenamer\apikeys.txt
```

على Windows عادة يكون:
```
C:\Users\[YourUsername]\AppData\Local\ImageAIRenamer\apikeys.txt
```

---

## استكشاف الأخطاء

### المشكلة: "No API keys found"

**الحل**: تأكد من إدخال مفاتيح API صحيحة في حقل API Configuration

### المشكلة: "Input folder does not exist"

**الحل**: تأكد من اختيار مجلد موجود يحتوي على صور

### المشكلة: الصور لم تُعاد تسميتها

**الحل**: 
- تحقق من أن الصور بصيغ مدعومة (.jpg, .jpeg, .png, .webp)
- تأكد من أن مفاتيح API لديها حد استخدام متبقي
- جرب إضافة مفتاح API آخر

---

## الصيغ المدعومة

- `.jpg`
- `.jpeg`
- `.png`
- `.webp`

---

## الترخيص

هذا المشروع مفتوح المصدر.

---

## الدعم

للمزيد من المعلومات عن Google Gemini API:
https://ai.google.dev/

---

## ملاحظات تقنية

### حجم الملف التنفيذي

- **الملف المستقل**: 139 MB
- يتضمن .NET Runtime كاملاً
- لا يحتاج لتثبيت أي متطلبات إضافية

### الإعدادات المستخدمة

```xml
<PublishSingleFile>true</PublishSingleFile>      <!-- ملف واحد -->
<SelfContained>true</SelfContained>              <!-- مستقل -->
<RuntimeIdentifier>win-x64</RuntimeIdentifier>   <!-- Windows 64-bit -->
<PublishReadyToRun>true</PublishReadyToRun>      <!-- محسّن للأداء -->
```

---

**آخر تحديث**: نوفمبر 2025
