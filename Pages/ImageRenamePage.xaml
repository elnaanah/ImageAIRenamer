<Page x:Class="ImageAIRenamer.Pages.ImageRenamePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:ImageAIRenamer.Models"
      mc:Ignorable="d"
        Title="إعادة تسمية الصور"
      Background="{StaticResource BackgroundColor}">
    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Back Button -->
        <Button Content="🏠 الصفحة الرئيسية" 
                Click="BackToHome_Click"
                Style="{StaticResource SecondaryButtonStyle}"
                HorizontalAlignment="Left"
                Margin="0,0,0,15"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="450"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <!-- API Keys -->
                <GroupBox Header="🔑 إعدادات API" Style="{StaticResource ModernGroupBoxStyle}">
                    <StackPanel>
                        <TextBlock Text="أدخل مفاتيح Gemini API (مفتاح واحد في كل سطر):" Margin="0,0,0,8" Foreground="{StaticResource TextPrimaryColor}" FontWeight="Medium"/>
                        <TextBox x:Name="ApiKeysBox" Height="70" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" 
                                 TextWrapping="NoWrap" Style="{StaticResource ModernTextBoxStyle}"
                                 ToolTip="أدخل مفاتيح Gemini API الخاصة بك، مفتاح واحد في كل سطر"/>
                    </StackPanel>
                </GroupBox>

                <!-- Folder Selection -->
                <GroupBox Header="📁 اختيار المجلدات" Style="{StaticResource ModernGroupBoxStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="15"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="مجلد الصور:" VerticalAlignment="Center" Grid.Row="0" Grid.Column="0" 
                                   Foreground="{StaticResource TextPrimaryColor}" FontWeight="Medium"/>
                        <TextBox x:Name="SourceFolderBox" Grid.Row="0" Grid.Column="1" Margin="12,0" Style="{StaticResource ModernTextBoxStyle}" IsReadOnly="True"/>
                        <Button Content="تصفح..." Click="BrowseSource_Click" Grid.Row="0" Grid.Column="2" 
                                Style="{StaticResource SecondaryButtonStyle}"/>

                        <TextBlock Text="مجلد الإخراج:" VerticalAlignment="Center" Grid.Row="2" Grid.Column="0" 
                                   Foreground="{StaticResource TextPrimaryColor}" FontWeight="Medium"/>
                        <TextBox x:Name="OutputFolderBox" Grid.Row="2" Grid.Column="1" Margin="12,0" Style="{StaticResource ModernTextBoxStyle}" IsReadOnly="True"/>
                        <Button Content="تصفح..." Click="BrowseOutput_Click" Grid.Row="2" Grid.Column="2" 
                                Style="{StaticResource SecondaryButtonStyle}"/>
                    </Grid>
                </GroupBox>

                <!-- Custom Instructions -->
                <GroupBox Header="🤖 تعليمات AI مخصصة" Style="{StaticResource ModernGroupBoxStyle}">
                    <StackPanel>
                        <TextBlock Text="أضف تعليمات إضافية للـ AI (اختياري):" Margin="0,0,0,6" 
                                   Foreground="{StaticResource TextPrimaryColor}" FontWeight="Medium"/>
                        <TextBox x:Name="CustomInstructionsBox" Height="70" 
                                 AcceptsReturn="True" VerticalScrollBarVisibility="Auto" TextWrapping="Wrap" 
                                 Style="{StaticResource ModernTextBoxStyle}"
                                 ToolTip="مثال: 'استخرج الاسم من النص الموجود أسفل الصورة' أو 'أعد التسمية بناءً على العنوان في الصورة'"/>
                    </StackPanel>
                </GroupBox>

                <!-- Actions -->
                <Border Background="{StaticResource SurfaceColor}" BorderBrush="{StaticResource BorderColor}" BorderThickness="1"
                        CornerRadius="6" Padding="18" Margin="0,5,0,0">
                    <StackPanel>
                        <TextBlock Text="الحالة" Foreground="{StaticResource TextSecondaryColor}" FontSize="12" FontWeight="SemiBold"/>
                        <TextBlock x:Name="StatusText" Margin="0,4,0,8" Foreground="{StaticResource TextPrimaryColor}" FontSize="13"/>
                        <ProgressBar x:Name="ProgressBar" Height="8" Visibility="Collapsed"
                                     Background="{StaticResource BorderColor}" Foreground="{StaticResource PrimaryColor}"/>
                        <Grid Margin="0,18,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Button Content="🗑️ مسح الكل" Click="ClearList_Click" 
                                    Style="{StaticResource SecondaryButtonStyle}" Grid.Column="0"/>
                            <Button x:Name="ProcessButton" Content="🚀 بدء إعادة التسمية" Click="Process_Click" 
                                    Style="{StaticResource ModernButtonStyle}" IsEnabled="False" Grid.Column="2"/>
                        </Grid>
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- Image List -->
            <GroupBox Header="🖼️ قائمة الصور" Grid.Column="2" Style="{StaticResource ModernGroupBoxStyle}">
                <DataGrid x:Name="ImagesGrid" AutoGenerateColumns="False" IsReadOnly="True" 
                          CanUserAddRows="False" CanUserResizeColumns="True"
                          BorderThickness="1" BorderBrush="{StaticResource BorderColor}"
                          Background="White" GridLinesVisibility="Horizontal" 
                          HorizontalGridLinesBrush="{StaticResource BorderColor}"
                          VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                          HeadersVisibility="Column" SelectionMode="Single">
                    <DataGrid.Resources>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Padding" Value="12,8"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#E3F2FD"/>
                                    <Setter Property="Foreground" Value="{StaticResource TextPrimaryColor}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#F8F9FA"/>
                            <Setter Property="Foreground" Value="{StaticResource TextPrimaryColor}"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="HorizontalContentAlignment" Value="Left"/>
                            <Setter Property="Padding" Value="12,10"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
                        </Style>
                    </DataGrid.Resources>
                    <DataGrid.Columns>
                        <DataGridTemplateColumn Header="معاينة" Width="120" CanUserResize="True">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Width="100" Height="80" BorderBrush="{StaticResource BorderColor}" BorderThickness="1">
                                        <Image Source="{Binding FilePath}" Stretch="Uniform" 
                                               MouseLeftButtonUp="Image_MouseLeftButtonUp" Cursor="Hand" ToolTip="انقر للفتح"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="الاسم الأصلي" Binding="{Binding OriginalName}" Width="200" CanUserResize="True">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="TextWrapping" Value="NoWrap"/>
                                    <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="الاسم الجديد" Binding="{Binding NewName}" Width="200" CanUserResize="True">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="TextWrapping" Value="NoWrap"/>
                                    <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="الحالة" Binding="{Binding Status}" Width="150" CanUserResize="True">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontWeight" Value="Medium"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Status}" Value="تم">
                                            <Setter Property="Foreground" Value="{StaticResource SuccessColor}"/>
                                            <Setter Property="FontWeight" Value="SemiBold"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Status}" Value="خطأ">
                                            <Setter Property="Foreground" Value="{StaticResource ErrorColor}"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Status}" Value="جاري إعادة التسمية...">
                                            <Setter Property="Foreground" Value="{StaticResource PrimaryColor}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
            </GroupBox>
        </Grid>
    </Grid>
</Page>
